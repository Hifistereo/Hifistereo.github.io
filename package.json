{
  "name": "kidmindpath-hub",
  "version": "1.0.0",
  "private": true,
  "description": "kidmindpath.com — the hub page and the shared design system",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
